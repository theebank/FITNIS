import { StyleSheet } from "react-native";

const PWDstyles = StyleSheet.create({
  innerContainerView: {
    display: "flex",
    flexDirection: "row",
    justifyContent: "space-between",
  },
  outercontainerView: {
    margin: 5,
  },
});

export default PWDstyles;
